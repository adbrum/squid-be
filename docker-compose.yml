version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3333:3333"
    volumes:
      - ./access.log:/app/access.log
    restart: unless-stopped
    environment:
      - NODE_ENV=production